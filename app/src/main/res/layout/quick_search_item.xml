<layout>
    <data>
        <variable name="artist" type="String" />
        <variable name="vm" type="com.android.kpopdance.viewmodel.SearchViewModel" />
    </data>
    <Button xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/singerButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginTop="15dp"
        android:layout_marginEnd="15dp"
        android:background="@drawable/singer_button_selector"
        android:text="@{artist}"
        android:onClick="@{() -> vm.onQuickSearchButtonClicked(artist)}"
        android:textColor="#FFFFFF"
        android:textSize="18sp" />
</layout>